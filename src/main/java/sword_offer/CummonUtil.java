package src.main.java.sword_offer;public class CummonUtil {    public static void main(String[] args) {        StringBuilder stringBuilder = generatePrim(26);        System.out.println(stringBuilder);    }    public static int numSubarrayProductLessThanK(int[] nums, int k) {        int count = 0;        int left = 0;        int sum = 1;        for (int right = 0; right < nums.length; right++) {            sum *= nums[right];            if (sum > k && left <= right) {                sum /= nums[left++];            } else if (sum <= k && left <= right) {                count++;            }        }        return count;    }    // 生成质数    public static StringBuilder generatePrim(int count) {        StringBuilder primeNums = new StringBuilder();        primeNums.append("{");        boolean flag = true;        for (int i = 2; i < 9999; i++) {            for (int j = 2; j <= Math.sqrt(i); j++) {                if (i % j == 0) {                    flag = false;                    break;                }            }            if (flag == true) {                primeNums.append(i + ",");                count--;                if(count==0){                    break;                }            }            flag = true;        }        primeNums.deleteCharAt(primeNums.length()-1);        primeNums.append("}");        return primeNums;    }}